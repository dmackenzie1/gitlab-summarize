[project]
name = "gitlab-summarize"
version = "0.1.0"
description = "Diff-grounded weekly GitLab engineering summary tooling"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.1",
    "requests>=2.31.0",
]
